<!DOCTYPE html>
<html lang="zh-Hant-TW">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AI 小工具</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body class="body">
    <form action="/" method="post">
      <div class="card">
        <h1 class="card-title">365天の生日代表花</h1>
        <p class="card-text">
          <% if (description.length) { %> <%= description %> <% } else { %> <%=
          '???' %> <% } %>
        </p>
        <input
          type="text"
          name="description"
          class="card-input"
          placeholder="請輸入生日找尋你的代表花"
        />
        <button type="submit" class="card-btn">送出</button>
      </div>
    </form>
  </body>
</html>
